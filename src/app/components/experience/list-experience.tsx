'use client'

import Experience from '@/app/components/experience/experience';

export default function ListExperience() {

    return (
        <div id={'exp'} className={'page text-textrest'}>
            <p>Skills</p>
            <Experience/>
            <Experience/>
            <Experience/>
        </div>
    )
}